# acml-project

Course project repository for music classification experiments on the FMA dataset.

This repository keeps the code and lightweight project files in Git, while excluding large datasets, trained model checkpoints, and local environment files that exceed GitHub limits.
